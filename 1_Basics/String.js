const name = "Likith"
const repoCount = 50

// `` - back ticks or template literal, ${} - using these two you can inject variables to the display
console.log(`Hello, my name is ${name} and my repo count is ${repoCount}`) 